<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tournament Framework</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <h1>🏆 Tournament Framework</h1>
        <p>Bot Competition Results</p>
    </header>

    <main>
        <% if (tournaments.length === 0) { %>
            <div class="empty-state">
                <h2>No tournaments found</h2>
                <p>Create a tournament using the CLI tool to get started:</p>
                <code>tournament create my-tournament</code>
            </div>
        <% } else { %>
            <div class="tournaments-grid">
                <% tournaments.forEach(tournament => { %>
                    <div class="tournament-card">
                        <h3><a href="/tournament/<%= tournament.name %>"><%= tournament.name %></a></h3>
                        <div class="tournament-stats">
                            <div class="stat">
                                <span class="stat-label">Participants:</span>
                                <span class="stat-value"><%= tournament.participants %></span>
                            </div>
                            <div class="stat">
                                <span class="stat-label">Matches:</span>
                                <span class="stat-value"><%= tournament.matches %></span>
                            </div>
                            <div class="stat">
                                <span class="stat-label">Status:</span>
                                <span class="stat-value status-<%= tournament.status %>"><%= tournament.status %></span>
                            </div>
                        </div>
                        <div class="tournament-meta">
                            <small>Created: <%= new Date(tournament.createdAt).toLocaleDateString() %></small>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } %>
    </main>

    <footer>
        <p>Tournament Framework v1.0.0 - Built with Node.js</p>
    </footer>

    <script src="/js/app.js"></script>
</body>
</html>