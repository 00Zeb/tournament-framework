// This is an intentionally malformed bot that doesn't follow the bot interface

class MalformedBot {
  // Missing constructor with proper signature
  
  // Missing makeMove method entirely
  
  someRandomMethod() {
    return 'This bot is broken';
  }
}

module.exports = MalformedBot;